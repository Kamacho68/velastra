version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "No frontend to build"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
